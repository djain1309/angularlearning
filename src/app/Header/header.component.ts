import { Component } from "@angular/core";

@Component({
    selector: 'app-header',
    template : '<h3>Learnen Angular</h3>'
})

// Without Decorator (@Component) : below is just a simple typescript class
// Using Decorator: it becomes a component
export class HeaderComponent {

}